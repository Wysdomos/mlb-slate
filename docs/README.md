# docs/

Reference material and build evidence. No code lives here.

## Documents

- **[PROMPT_Session_Report.md](PROMPT_Session_Report.md)** — the standard for
  builder write-ups: header block, scope, root cause, lettered verification
  with pasted output. Includes the rule that each builder writes to
  `SESSION_STATUS_<topic>.md`, never the shared `SESSION_STATUS.md`.

- **[DISCOVERY_CHAPTER_D.md](DISCOVERY_CHAPTER_D.md)** — Chapter D discovery,
  run 2026-07-25. Per workbook tab, what the BallparkPal API can and cannot
  source, with live pull evidence. This is the investigation behind BP_Teams
  and BP_Games staying workbook-only. Historical record: findings reflect the
  API as of that date.

## Build evidence

Screenshots and rendered pages kept as the record for a specific piece of work.
Each is referenced from the matching `SESSION_STATUS_*.md` in the repo root.

- **[preview/](preview/)** — `projected.html`, a fully built Projected Mode
  page from a real 15-game slate, self-contained so it can be opened directly
  on a phone. Sits alongside the icons and QR images the page references.

- **[projected-header-withheld/](projected-header-withheld/)** — before/after
  captures for the Projected Mode header safe-area fix, the withheld-boards
  disclosure, and the light/dark theme work.

- **[chapter-h/](chapter-h/)** — K board mobile columns, Chapter H.

- **[render-fix/](render-fix/)** — parlay surfaces after the render defects fix.

- **[projected-mode-sample.png](projected-mode-sample.png)** — early Projected
  Mode reference capture.
